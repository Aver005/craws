//! Craws MCP server — lands in M1 on the official `rmcp` SDK.
//!
//! This stub exists so the workspace has its final shape from day one: CLI,
//! MCP and GUI are equal ports over `craws-engine`. Planned first tools:
//! `open_image`, `resize`, `crop`, `apply_filter`, `export`, `describe_image`.

pub const SERVER_NAME: &str = "craws";
